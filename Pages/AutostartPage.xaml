<Page x:Class="SysPilot.Pages.AutostartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:converters="clr-namespace:SysPilot.Converters"
      xmlns:local="clr-namespace:SysPilot"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="600"
      Background="Transparent"
      Title="{x:Static local:Lang.AutostartTitle}">

    <Page.Resources>
        <converters:NullToVisibleConverter x:Key="NullToVisibleConverter"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="track"
                                    Width="40" Height="22"
                                    CornerRadius="11"
                                    Background="#404040"/>
                            <Border x:Name="thumb"
                                    Width="18" Height="18"
                                    CornerRadius="9"
                                    Background="#808080"
                                    HorizontalAlignment="Left"
                                    Margin="2,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="#007ACC"/>
                                <Setter TargetName="thumb" Property="Background" Value="#FFFFFF"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="0,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{x:Static local:Lang.AutostartTitle}"
                   Foreground="#E0E0E0"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <!-- Search -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Background="#252525" CornerRadius="6" Padding="12,8">
                <Grid>
                    <TextBlock x:Name="SearchPlaceholder"
                               Text="{x:Static local:Lang.SearchAutostart}"
                               Foreground="#606060"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"/>
                    <TextBox x:Name="SearchBox"
                             Background="Transparent"
                             Foreground="#E0E0E0"
                             BorderThickness="0"
                             CaretBrush="#E0E0E0"
                             FontSize="13"
                             VerticalAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                </Grid>
            </Border>

            <Button Grid.Column="1"
                    Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0"
                    Click="Refresh_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Static local:Lang.Refresh}" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Autostart List -->
        <Border x:Name="AutostartListBorder" Grid.Row="2" Background="#1E1E1E" CornerRadius="12" SizeChanged="Border_SizeChanged">
            <Grid>
                <DataGrid x:Name="AutostartList"
                      AutoGenerateColumns="False"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="#E0E0E0"
                      RowBackground="Transparent"
                      AlternatingRowBackground="#222222"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True"
                      SelectionChanged="AutostartList_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{x:Static local:Lang.Status}" Width="80" SortMemberPath="IsEnabled" CanUserSort="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Style="{StaticResource ToggleSwitch}"
                                          IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                          Click="Toggle_Click"
                                          HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="{x:Static local:Lang.Name}" Width="180" SortMemberPath="Name" CanUserSort="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="8,4">
                                    <Grid Width="16" Height="16" VerticalAlignment="Center">
                                        <Image Source="{Binding Icon}" Width="16" Height="16" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <iconPacks:PackIconMaterial Kind="Application" Width="14" Height="14" Foreground="#606060"
                                                                    Visibility="{Binding Icon, Converter={StaticResource NullToVisibleConverter}}"/>
                                    </Grid>
                                    <TextBlock Text="{Binding Name}" Margin="8,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="{x:Static local:Lang.Source}" Width="150" SortMemberPath="Location" CanUserSort="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2D4A6D"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSystemWide}" Value="True">
                                                    <Setter Property="Background" Value="#4A3D6D"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Source}" Value="TaskScheduler">
                                                    <Setter Property="Background" Value="#4A6D4A"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Source}" Value="StartupFolder">
                                                    <Setter Property="Background" Value="#6D5A2D"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Location}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{x:Static local:Lang.Command}" Width="*" Binding="{Binding Command}" SortMemberPath="Command" CanUserSort="True"/>
                </DataGrid.Columns>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E1E1E"/>
                        <Setter Property="Foreground" Value="#808080"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#303030"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <Grid>
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="Center"/>
                                            <Thumb x:Name="PART_RightHeaderGripper"
                                                   HorizontalAlignment="Right" Width="2"
                                                   Cursor="SizeWE" Opacity="0"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>

                <!-- Loading indicator -->
                <Border x:Name="LoadingPanel" Background="#CC1E1E1E" CornerRadius="12" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <iconPacks:PackIconMaterial x:Name="LoadingIcon"
                                                    Kind="Sync"
                                                    Width="32" Height="32"
                                                    Foreground="#007ACC"
                                                    HorizontalAlignment="Center"
                                                    RenderTransformOrigin="0.5,0.5">
                            <iconPacks:PackIconMaterial.RenderTransform>
                                <RotateTransform/>
                            </iconPacks:PackIconMaterial.RenderTransform>
                        </iconPacks:PackIconMaterial>
                        <TextBlock Text="{x:Static local:Lang.LoadingAutostart}"
                                   Foreground="#808080"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button x:Name="DeleteButton"
                    Style="{StaticResource ActionButton}"
                    IsEnabled="False"
                    Click="Delete_Click">
                <StackPanel Orientation="Horizontal">
                    <Grid>
                        <iconPacks:PackIconMaterial Kind="Delete" Width="14" Height="14"/>
                        <iconPacks:PackIconMaterial Kind="Shield" Width="8" Height="8"
                                                    Foreground="#F0C030"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Bottom"
                                                    Margin="0,0,-3,-2"/>
                    </Grid>
                    <TextBlock Text="{x:Static local:Lang.Remove}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <TextBlock x:Name="EntryCountText"
                       Foreground="#606060"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
