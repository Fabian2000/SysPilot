<Page x:Class="SysPilot.Pages.CleanupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:SysPilot"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="600"
      Background="Transparent"
      Title="{x:Static local:Lang.CleanupTitle}">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E8AD2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CleanupCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box"
                                    Width="18" Height="18"
                                    CornerRadius="4"
                                    Background="#252525"
                                    BorderBrush="#404040"
                                    BorderThickness="1">
                                <iconPacks:PackIconMaterial x:Name="check"
                                                            Kind="Check"
                                                            Width="12" Height="12"
                                                            Foreground="#007ACC"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="Background" Value="#303030"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="0,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{x:Static local:Lang.CleanupTitle}"
                   Foreground="#E0E0E0"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="{x:Static local:Lang.CleanupDescription}"
                   Foreground="#606060"
                   FontSize="13"
                   Margin="0,0,0,20"/>

        <!-- Cleanup Targets List -->
        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="12" Padding="16">
            <Grid>
                <ListView x:Name="TargetsList"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="#E0E0E0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0"
                                          Style="{StaticResource CleanupCheckBox}"
                                          IsChecked="{Binding IsSelected}"
                                          VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding Name}" Foreground="#E0E0E0" FontSize="13"/>
                                    <TextBlock Text="{Binding Path}" Foreground="#606060" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2"
                                           Text="{Binding SizeFormatted}"
                                           Foreground="#808080"
                                           FontSize="13"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="8"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}"
                                                CornerRadius="6">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TargetItem_Click"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <!-- Loading indicator -->
                <StackPanel x:Name="LoadingPanel"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="Collapsed">
                    <iconPacks:PackIconMaterial x:Name="LoadingIcon"
                                                Kind="Sync"
                                                Width="32" Height="32"
                                                Foreground="#007ACC"
                                                HorizontalAlignment="Center"
                                                RenderTransformOrigin="0.5,0.5">
                        <iconPacks:PackIconMaterial.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation"/>
                        </iconPacks:PackIconMaterial.RenderTransform>
                    </iconPacks:PackIconMaterial>
                    <TextBlock x:Name="LoadingText"
                               Text="{x:Static local:Lang.Scanning}"
                               Foreground="#606060"
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Progress -->
        <Border x:Name="ProgressPanel" Grid.Row="3" Margin="0,16,0,0" Visibility="Collapsed">
            <StackPanel>
                <Grid>
                    <ProgressBar x:Name="ProgressBar"
                                 Height="6"
                                 Background="#252525"
                                 Foreground="#007ACC"
                                 BorderThickness="0"/>
                </Grid>
                <TextBlock x:Name="ProgressText"
                           Text="{x:Static local:Lang.Cleaning}"
                           Foreground="#808080"
                           FontSize="12"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Style="{StaticResource ActionButton}"
                        Click="Refresh_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Static local:Lang.Scan}" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ActionButton}"
                        Margin="8,0,0,0"
                        Click="SelectAll_Click">
                    <TextBlock Text="{x:Static local:Lang.SelectAll}"/>
                </Button>

                <Button Style="{StaticResource ActionButton}"
                        Margin="8,0,0,0"
                        Click="SelectNone_Click">
                    <TextBlock Text="{x:Static local:Lang.SelectNone}"/>
                </Button>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock x:Name="TotalSizeText"
                           Foreground="#808080"
                           FontSize="13"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0"/>

                <Button x:Name="CleanButton"
                        Style="{StaticResource PrimaryButton}"
                        Click="Clean_Click">
                    <StackPanel Orientation="Horizontal">
                        <Grid>
                            <iconPacks:PackIconMaterial Kind="Broom" Width="14" Height="14" VerticalAlignment="Center"/>
                            <iconPacks:PackIconMaterial Kind="Shield" Width="8" Height="8"
                                                        Foreground="#F0C030"
                                                        HorizontalAlignment="Right"
                                                        VerticalAlignment="Bottom"
                                                        Margin="0,0,-3,-2"/>
                        </Grid>
                        <TextBlock Text="{x:Static local:Lang.Clean}" Margin="8,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
