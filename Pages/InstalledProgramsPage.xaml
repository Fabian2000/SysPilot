<Page x:Class="SysPilot.Pages.InstalledProgramsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:SysPilot"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Background="Transparent"
      Title="{x:Static local:Lang.ProgramsTitle}">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#4A2020"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6A3030"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box"
                                    Width="18" Height="18"
                                    CornerRadius="4"
                                    Background="#252525"
                                    BorderBrush="#404040"
                                    BorderThickness="1">
                                <iconPacks:PackIconMaterial x:Name="check"
                                                            Kind="Check"
                                                            Width="12" Height="12"
                                                            Foreground="#007ACC"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="Background" Value="#303030"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="0,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{x:Static local:Lang.ProgramsTitle}"
                   Foreground="#E0E0E0"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="{x:Static local:Lang.ProgramsSubtitle}"
                       Foreground="#606060"
                       FontSize="13"
                       VerticalAlignment="Center"/>
            <TextBlock x:Name="LoadTimeText"
                       Foreground="#007ACC"
                       FontSize="13"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search -->
            <Border Background="#252525" CornerRadius="6" Padding="12,8" Margin="0,0,16,0">
                <Grid>
                    <TextBlock x:Name="SearchPlaceholder"
                               Text="{x:Static local:Lang.SearchPrograms}"
                               Foreground="#606060"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"/>
                    <TextBox x:Name="SearchBox"
                             Background="Transparent"
                             Foreground="#E0E0E0"
                             BorderThickness="0"
                             CaretBrush="#E0E0E0"
                             FontSize="13"
                             VerticalAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                </Grid>
            </Border>

            <!-- Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <!-- Show System Components -->
                <CheckBox x:Name="ShowSystemCheckbox"
                          Style="{StaticResource DarkCheckBox}"
                          Content="{x:Static local:Lang.ShowSystemComponents}"
                          VerticalAlignment="Center"
                          Margin="0,0,16,0"
                          Checked="ShowSystem_Changed"
                          Unchecked="ShowSystem_Changed"/>

                <!-- Stats -->
                <Border Background="#252525" CornerRadius="6" Padding="12,6" Margin="0,0,8,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="ProgramCountText" Text="0 programs" Foreground="#808080" FontSize="12"/>
                        <TextBlock Text=" | " Foreground="#404040" FontSize="12"/>
                        <TextBlock x:Name="TotalSizeText" Text="0 GB" Foreground="#808080" FontSize="12"/>
                    </StackPanel>
                </Border>

                <Button x:Name="UninstallButton" Style="{StaticResource DangerButton}"
                        IsEnabled="False"
                        Click="Uninstall_Click" ToolTip="Uninstall selected program">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="DeleteOutline" Width="14" Height="14" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Static local:Lang.Uninstall}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ActionButton}" Click="Refresh_Click" Margin="8,0,0,0" ToolTip="Refresh list">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Programs List -->
        <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="12" SizeChanged="Border_SizeChanged">
            <Grid>
                <!-- Loading Overlay -->
                <Border x:Name="LoadingPanel" Background="#1E1E1E" CornerRadius="12" Visibility="Collapsed" Panel.ZIndex="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <iconPacks:PackIconMaterial x:Name="LoadingIcon" Kind="Sync" Width="32" Height="32"
                                                    Foreground="#007ACC" HorizontalAlignment="Center"
                                                    RenderTransformOrigin="0.5,0.5">
                            <iconPacks:PackIconMaterial.RenderTransform>
                                <RotateTransform Angle="0"/>
                            </iconPacks:PackIconMaterial.RenderTransform>
                        </iconPacks:PackIconMaterial>
                        <TextBlock Text="{x:Static local:Lang.LoadingPrograms}" Foreground="#808080" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>

                <ListView x:Name="ProgramsList"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="#E0E0E0"
                          SelectionChanged="ProgramsList_SelectionChanged"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="16,10"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#0D3A5C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="90"/>
                                </Grid.ColumnDefinitions>

                                <!-- Name & Publisher -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Foreground="#E0E0E0" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Publisher}" Foreground="#606060" FontSize="11" TextTrimming="CharacterEllipsis"
                                               Visibility="{Binding Publisher, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>

                                <!-- Version -->
                                <TextBlock Grid.Column="1" Text="{Binding Version}" Foreground="#808080" FontSize="12"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                <!-- Size -->
                                <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" Foreground="#808080" FontSize="12"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"/>

                                <!-- Install Date -->
                                <TextBlock Grid.Column="3" Text="{Binding InstallDateFormatted}" Foreground="#606060" FontSize="11"
                                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
