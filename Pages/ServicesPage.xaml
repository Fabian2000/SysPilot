<Page x:Class="SysPilot.Pages.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:SysPilot"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="600"
      Background="Transparent"
      Title="{x:Static local:Lang.ServicesTitle}">

    <Page.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="0,0,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{x:Static local:Lang.ServicesTitle}"
                   Foreground="#E0E0E0"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <!-- Search -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Background="#252525" CornerRadius="6" Padding="12,8">
                <Grid>
                    <TextBlock x:Name="SearchPlaceholder"
                               Text="{x:Static local:Lang.SearchServices}"
                               Foreground="#606060"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"/>
                    <TextBox x:Name="SearchBox"
                             Background="Transparent"
                             Foreground="#E0E0E0"
                             BorderThickness="0"
                             CaretBrush="#E0E0E0"
                             FontSize="13"
                             VerticalAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                </Grid>
            </Border>

            <Button Grid.Column="1"
                    Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0"
                    Click="Refresh_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Static local:Lang.Refresh}" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Services List -->
        <Border x:Name="ServicesListBorder" Grid.Row="2" Background="#1E1E1E" CornerRadius="12" SizeChanged="Border_SizeChanged">
            <DataGrid x:Name="ServicesList"
                      AutoGenerateColumns="False"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="#E0E0E0"
                      RowBackground="Transparent"
                      AlternatingRowBackground="#222222"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True"
                      SelectionChanged="ServicesList_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{x:Static local:Lang.Name}" Width="220" SortMemberPath="DisplayName" CanUserSort="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="8,4">
                                    <iconPacks:PackIconMaterial Kind="Cog" Width="14" Height="14" Foreground="#606060" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding DisplayName}" Margin="8,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="{x:Static local:Lang.Status}" Width="100" SortMemberPath="Status" CanUserSort="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#404040"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                                    <Setter Property="Background" Value="#1E5631"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Stopped">
                                                    <Setter Property="Background" Value="#4A2020"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{x:Static local:Lang.Startup}" Width="90" Binding="{Binding StartType}" SortMemberPath="StartType" CanUserSort="True"/>
                    <DataGridTextColumn Header="{x:Static local:Lang.ServiceName}" Width="*" Binding="{Binding Name}" SortMemberPath="Name" CanUserSort="True"/>
                </DataGrid.Columns>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E1E1E"/>
                        <Setter Property="Foreground" Value="#808080"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#303030"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <Grid>
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="Center"/>
                                            <Thumb x:Name="PART_RightHeaderGripper"
                                                   HorizontalAlignment="Right" Width="2"
                                                   Cursor="SizeWE" Opacity="0"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button x:Name="StartButton"
                    Style="{StaticResource ActionButton}"
                    IsEnabled="False"
                    Click="Start_Click">
                <StackPanel Orientation="Horizontal">
                    <Grid>
                        <iconPacks:PackIconMaterial Kind="Play" Width="14" Height="14"/>
                        <iconPacks:PackIconMaterial Kind="Shield" Width="8" Height="8"
                                                    Foreground="#F0C030"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Bottom"
                                                    Margin="0,0,-3,-2"/>
                    </Grid>
                    <TextBlock Text="{x:Static local:Lang.Start}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button x:Name="StopButton"
                    Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0"
                    IsEnabled="False"
                    Click="Stop_Click">
                <StackPanel Orientation="Horizontal">
                    <Grid>
                        <iconPacks:PackIconMaterial Kind="Stop" Width="14" Height="14"/>
                        <iconPacks:PackIconMaterial Kind="Shield" Width="8" Height="8"
                                                    Foreground="#F0C030"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Bottom"
                                                    Margin="0,0,-3,-2"/>
                    </Grid>
                    <TextBlock Text="{x:Static local:Lang.Stop}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button x:Name="RestartButton"
                    Style="{StaticResource ActionButton}"
                    Margin="8,0,0,0"
                    IsEnabled="False"
                    Click="Restart_Click">
                <StackPanel Orientation="Horizontal">
                    <Grid>
                        <iconPacks:PackIconMaterial Kind="Restart" Width="14" Height="14"/>
                        <iconPacks:PackIconMaterial Kind="Shield" Width="8" Height="8"
                                                    Foreground="#F0C030"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Bottom"
                                                    Margin="0,0,-3,-2"/>
                    </Grid>
                    <TextBlock Text="{x:Static local:Lang.Restart}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <TextBlock x:Name="ServiceCountText"
                       Foreground="#606060"
                       FontSize="12"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
