<UserControl x:Class="SysPilot.Controls.SidebarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="44" d:DesignWidth="200"
             Cursor="Hand">

    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border x:Name="ButtonBorder" Style="{StaticResource SidebarButtonStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Selection Indicator -->
            <Border x:Name="SelectionIndicator"
                    Grid.Column="0"
                    Width="3"
                    HorizontalAlignment="Left"
                    Margin="-12,0,0,0"
                    CornerRadius="0,2,2,0"
                    Background="#007ACC"
                    Visibility="Collapsed"/>

            <ContentPresenter x:Name="IconPresenter"
                              Grid.Column="0"
                              Content="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>

            <TextBlock x:Name="LabelText"
                       Grid.Column="1"
                       Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="#B0B0B0"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"/>
        </Grid>
    </Border>
</UserControl>
